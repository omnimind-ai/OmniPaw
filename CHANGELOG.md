# Changelog

## 0.2.0

### 新增

- 新增网络搜索，支持 Tavily、博查、Brave Search、Firecrawl、百度 AI Search 与 Exa。设置页支持保存各服务商密钥、切换服务商、限制结果数量、配置 Tavily 搜索深度及测试连接。
- 聊天回答新增网页来源引用，支持行内引用标记、站点图标、来源面板和原网页访问；Markdown 文件预览也会保留同一条消息中的引用信息。
- 新增流式响应开关，模型生成期间可逐步显示回复，并支持网络异常重试与响应流恢复提示。
- Agent 完成一轮任务后会汇总本轮修改的工作区文件，可展开查看文件内容和 Markdown 预览。
- 新增 MP3 与 WAV 音频附件传送能力，支持音频输入的模型可读取附件内容。

### 改进

- 调整历史消息的继续生成行为。应用会从指定的用户消息重新生成上下文，并将其后的旧回复设为替代状态，避免旧内容再次进入模型上下文。
- 完善本地 Agent 的文件访问与终端权限管理，支持授权外部目录，并分别执行命令白名单、命令黑名单、后台任务及网络访问策略。
- 改善视觉观察的模型候选顺序、回应解析与回应策略；“观察并测试回应”会请求一次可见回应，便于检查当前配置。
- 使用 sqlite-vec 建立陪伴记忆向量索引，并在记忆新增或更新后自动刷新索引，提升语义检索效率。
- 计划任务根据各任务自身状态运行，策略设置保留补跑规则；任务完成或失败后会显示可关闭的桌宠气泡。
- 改善模型服务可用性检查以及兼容 OpenAI 接口的工具调用处理，服务缺少工具调用 ID 时会自动补充本地 ID。
- 输入框增加最大高度与滚动区域，长消息编辑时占用的界面空间更加合理。

### 修复

- 修正刚发送的图片与媒体附件仅显示文件名的问题，发送后会立即加载对应预览。
- 修正 Windows 启动初期可能暂时无法获取桌面截图来源的问题，应用会等待来源可用后继续截图。
- 修正网络搜索设置草稿共享嵌套状态的问题，使草稿与存储配置保持独立。
- 修正部分模型缩短网页引用标识后来源信息无法显示的问题。

## 0.1.1

### 新增

- 新增桌面伙伴 Nori Quill，包含完整角色设定、专属动态形象、互动内容与三件好感度礼物。
- 新增应用更新检查。应用启动时会自动检查新版本，也可在“关于”页面手动检查；发现新版本后会展示版本号、发布日期、更新内容与下载入口。

### 改进

- 改善本地任务的进程管理。删除会话或退出应用时会终止相关任务及其子进程，并按设置清理会话工作区，减少后台进程残留。
- 统一应用数据目录的解析方式，区分开发环境与正式版本的数据，并支持通过 `OMNIPAW_DATA_DIR` 指定自定义目录。
- 优化模型服务设置页的空状态与搜索无结果提示，使新建入口和当前状态更加清晰。
- 更新 Nori Quill 的动态形象素材与礼物配置。

### 修复

- 修正默认更新服务地址，确保版本检查请求发送到有效端点。
- 修正任务超时、中止及会话删除后，部分子进程仍可能继续运行的问题。

## 0.1.0

### 首次发布

OmniPaw 首次版本
